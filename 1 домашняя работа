#Написать программу, которая запрашивает на ввод целые числа, до тех пор, пока не будет введена пустая строка.
#Затем она выводит: а) введенную последовательность чисел; б) сумма введенных чисел; в) самое большое число из введенных.
#(а), (б) и (в) - это разные варианты одной программы.

def numbers1(modes: str = "abc"):
    numbers = []
    while True:
        line = input("Введите число: ")
        if line == "":
            break
        numbers.append(int(line))
    if "a" in modes:
        print("Введенная последовательность чисел-",  numbers )
    if "b" in modes:
        print("Сумма введенных чисел-", sum(numbers))
    if "c" in modes:
        print("Самое большое число из введенных-", max(numbers))
numbers1()
